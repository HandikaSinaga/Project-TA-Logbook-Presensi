APP_PORT=3001
NODE_ENV=production
CLIENT_URL=http://YOUR_DOMAIN_OR_IP

DB_HOST=YOUR_RDS_ENDPOINT_OR_LOCALHOST
DB_USER=admin
DB_PASSWORD=YOUR_STRONG_PASSWORD
DB_NAME=db_presensi_ta

JWT_SECRET=GENERATE_64_CHAR_RANDOM_STRING
ACCESS_TOKEN_SECRET=GENERATE_64_CHAR_RANDOM_STRING
REFRESH_TOKEN_SECRET=GENERATE_64_CHAR_RANDOM_STRING
RESET_PASSWORD_SECRET=GENERATE_64_CHAR_RANDOM_STRING

EMAIL_USER=
EMAIL_PASS=

# Google OAuth Configuration
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
GOOGLE_REDIRECT_URI=http://YOUR_DOMAIN_OR_IP/api/auth/google/callback
OAUTH_ALLOWED_HD=

# ============================================
# PRODUCTION SETUP GUIDE
# ============================================
# 1. Copy to .env: cp .env.example .env
# 2. Generate secrets: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# 3. Update CLIENT_URL with your domain/IP
# 4. Update DB_HOST with RDS endpoint
# 5. Set strong DB_PASSWORD
# 6. Update GOOGLE_REDIRECT_URI with your domain/IP
# 
# NOTE: Frontend automatically uses /api for production builds
# No need to configure frontend environment variables
